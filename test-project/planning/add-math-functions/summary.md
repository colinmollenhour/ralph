# Add Missing Math Functions

## Overview
Complete the math utility module (`src/math.ts`) by implementing the five missing functions: multiply, divide, power, modulo, and absolute value.

## Current State
- `src/math.ts` exports `add()` and `subtract()` functions
- Five TODO comments indicate the missing functions
- TypeScript strict mode is enabled; `npm run typecheck` is the quality gate

## Goals
1. Implement all five math functions with correct signatures and behavior
2. Remove the corresponding TODO comments
3. Pass TypeScript typecheck (`tsc --noEmit`)

## Scope
- Only `src/math.ts` needs modification
- No new files or dependencies required
- No test files (typecheck is sufficient validation)
